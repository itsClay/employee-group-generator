<script type="text/javascript">
  <% if is_logged_in? %>
    window.currentUser = <%= render(
      "/api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
</script>

# planning on implementing react if we have enough time. So setting up a 
# hook for root

<main id="root">
react not working
</main>
